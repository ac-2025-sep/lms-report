"""Legacy module retained for historical reference.

The project has been refactored into a reusable Django app package.
Use `userops_reports` and include `userops_reports.urls` in your LMS URL config.
"""

raise RuntimeError(
    "FastAPI runtime removed. Integrate the Django app `userops_reports` instead."
)
